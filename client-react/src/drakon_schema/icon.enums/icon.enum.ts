export const IconTypes = {
    BEGIN : "BEGIN",
    END : "END",
    ACTION : "ACTION",
    IF : "IF",
    ENDIF: "ENDIF",
    ELSE : "ELSE",
    SWITCH : "MACRO_WHILE",
    IMPUT : "INPUT",
    OUTPUT : "OUTPUT",
    PAUSE : "PAUSE",
    COMMENT : "COMMENT",
    SEPARATOR : "SEPARATOR"
} as const